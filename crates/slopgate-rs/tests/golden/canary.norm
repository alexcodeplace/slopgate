⚠ SLOPGATE: ast-grep: ast-grep: using PATH binary (version not pinned — results may differ from CI)

╔═ SLOPGATE ═════════════════════════════════════════╗
║ SLOPGATE — VIOLATIONS IN EDITED FILE               ║
╚═════════════════════════════════════════════════════╝

── ast ──
[HIGH] slopgate-canary rules/baseline/fixtures/src/canary.tsx:6
  × export const x = __SLOPGATE_AST_CANARY__;
  ✓ self-test only — must only ever match fixtures/

[HIGH] inner-html-assignment rules/baseline/fixtures/src/canary.tsx:14
  × function bad(el: HTMLElement) { el.innerHTML = '<b>hi</b>'; }
  ✓ Build DOM via safe APIs or sanitize with isomorphic-dompurify.

[HIGH] inner-html-assignment rules/baseline/fixtures/src/canary.tsx:15
  × const _dangerous = <div dangerouslySetInnerHTML={{ __html: html }} />;
  ✓ Build DOM via safe APIs or sanitize with isomorphic-dompurify.

[CRITICAL] focused-test-only-tsx rules/baseline/fixtures/src/canary.tsx:17
  × describe.only('focused suite canary', () => {});
  ✓ Remove .only before committing so the full test suite runs.

[CRITICAL] focused-test-shorthand-tsx rules/baseline/fixtures/src/canary.tsx:18
  × fdescribe('focused suite shorthand canary', () => {});
  ✓ Replace fit/fdescribe with test/it/describe before committing so the full test suite runs.

── regex ──
[CRITICAL] no-stubs-placeholder rules/baseline/fixtures/src/canary.tsx:3
  × // placeholder for now
  ✓ Implement the real behavior now; remove the placeholder.

[CRITICAL] no-stubs-placeholder rules/baseline/fixtures/src/canary.tsx:4
  × // TODO: implement later
  ✓ Implement the real behavior now; remove the placeholder.

[CRITICAL] no-stubs-placeholder rules/baseline/fixtures/src/canary.tsx:5
  × // not implemented
  ✓ Implement the real behavior now; remove the placeholder.

[HIGH] ts-suppress-added rules/baseline/fixtures/src/canary.tsx:12
  × // @ts-ignore
  ✓ Fix the underlying type error; remove the suppression.

[CRITICAL] live-secret-literal rules/baseline/fixtures/src/canary.tsx:22
  × const _sk = "sk_live_aaaaaaaaaaaa";
  ✓ Move to a secret manager or environment variable; never commit live keys.

[CRITICAL] eval-dynamic-code rules/baseline/fixtures/src/canary.tsx:24
  × // eval(userCode);  — commented to avoid actual eval in fixture
  ✓ Eliminate eval(); use static dispatch tables or data-driven config.

[CRITICAL] eval-dynamic-code rules/baseline/fixtures/src/canary.tsx:25
  × const _evalStr = 'eval(userCode)';
  ✓ Eliminate eval(); use static dispatch tables or data-driven config.

[HIGH] pii-in-logs rules/baseline/fixtures/src/canary.tsx:27
  × console.log(user.phone, user.email);
  ✓ Remove log or redact sensitive fields before logging.

[HIGH] weak-hash-algorithm rules/baseline/fixtures/src/canary.tsx:29
  × // createHash('md5') — weak hash
  ✓ Use SHA-256+ for integrity; bcrypt/argon2/scrypt for passwords.

[HIGH] weak-hash-algorithm rules/baseline/fixtures/src/canary.tsx:30
  × const _wh = "createHash('md5').update(data)";
  ✓ Use SHA-256+ for integrity; bcrypt/argon2/scrypt for passwords.

15 violation(s) in 1 file(s). Fix now while context is hot.
False positive? NEVER edit suppressions.json yourself — ask the user via AskUserQuestion.

